name: cdl_dash #Environment name
dependencies:
  - python=3.7
  - pip:
    - dash
    - dash-renderer
    - dash-core-components
    - dash-html-components
    - plotly
    - gunicorn # for app deployment
    - pandas
    - dash_table
    - dash_bootstrap_components